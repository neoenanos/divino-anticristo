---
export interface Props {
  date?: Date;
}

const { date } = Astro.props;
let _date = date || new Date(-8640000)
const formattedDate = _date.toISOString().split('-')[0];
---

<time datetime={_date.toISOString()}>
  {formattedDate}
</time>