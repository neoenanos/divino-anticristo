---
export interface Props {
  date?: Date;
}

const { date } = Astro.props;
const formattedDate = date?.toISOString().split('-')[0];
---

{
  date && <time datetime={date.toISOString()}>
  {formattedDate}
  </time>
}